# local run state + generated proof artifacts (regenerable; never publish)
# .super-loop/ holds real mined-corpus paths + run state; proof/ is demo/run output.
.super-loop/
.super-loop*/
proof/
reports/
node_modules/
__pycache__/
*.pyc
*.log
.DS_Store

# local working backups (never publish)
.backup-*
*.backup
src.bak-*/

# internal operator work orders (never publish)
docs/CODEX_REAL_TEST_TIER1_REPAIR_PROMPT.md

# secrets / scratchpad keys (never publish)
.keys.env
*.keys.env
.env
.env.*

# local edit backups — never publish
*.bak-*
